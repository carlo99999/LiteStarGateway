.env
.env.docker-dev
.env.benchmark
docker-compose.load.yml
.docker-compose.load.tmp.*
*.db
.venv/
__pycache__/
.pytest_cache/

# MkDocs generated source projection and build output
.mkdocs-docs/
.mkdocs-site/
.hypothesis
# Coverage artifacts (pytest-cov)
.coverage
.coverage.*
htmlcov/
load-results/

## Graphify: track only graph.json + GRAPH_REPORT.md; everything else is
## generated/local snapshot data (rebuilt by `graphify update .`).
graphify-out/
# Claude Code session worktrees (machine-local, never committed)
.claude/worktrees/

.ruff_cache
.rumdl_cache
.claude-plugin

# Admin UI (ui/): dependencies, build output, and local caches.
ui/node_modules/
ui/dist/
ui/*.tsbuildinfo
ui/.vite/

# Playwright E2E: throwaway backend database, reports, and failure artifacts.
.e2e-data/
ui/playwright-report/
ui/test-results/
ui/blob-report/

**/*/.DS_Store
